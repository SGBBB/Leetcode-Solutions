Time Compolexity ananlysis:
​
**Time**: O(m), where m is the number of connections (union operations). Since we are using the path compression, a single union operation is O(log*(m))
​
​
**Memory**: O(n) for the disjoint set.